# Copy this file to .env and fill it ONLY on your own machine.
# Use a newly issued key if a previous key was exposed in a screenshot.
OPENAI_API_KEY=
# Base URL from the SAME provider that serves the model below.
# Do not assume the model alias is available at another provider's URL.
OPENAI_API_BASE=
MODEL_NAME=deepseek-flash
